import { UploadThingError } from '@uploadthing/shared';
import { FileRouter, inferErrorShape } from '../../types/index.js';

declare function defaultErrorFormatter(error: UploadThingError): {
    message: string;
};
declare function formatError(error: UploadThingError, router: FileRouter): inferErrorShape<FileRouter[string]>;

export { defaultErrorFormatter, formatError };
